node_modules/
data/
embeddings/          # retrieval indexes — contain chunked attachment TEXT; never commit
logs/
dist/
*.log
.mechape-signing/     # code-signing PFX + password; never commit
.DS_Store
Thumbs.db

# llama.cpp runtime fetched at build time (scripts/fetch-runtime.js)
electron/runtime/
